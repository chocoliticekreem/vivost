# Codeplain CLI (cloned third-party repo, has its own git history)
plain2code_client/

# Dependencies & build artifacts
node_modules/
dist/
plain_modules/
conformance_tests/

# Logs / env / tool output
*.log
.env
.playwright-mcp/

# Real scraped listing data — personal data, never commit (regenerate locally)
build/src/data/profiles.generated.ts
scraper/urls.json
scraper/data/
# Screenshots may contain real listing photos
shots/redesign-*.png
.vercel

